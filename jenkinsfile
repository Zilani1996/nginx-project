pipeline{
agent any
stages{
  stage("code clone"){
   steps{
    git url :"https://github.com/Zilani1996/nginx-project",branch:"master"
    }
  }
  stage("code build"){
    steps{
          echo "Build the image"
         sh "docker build -t server5 ."
        } 
    }
     stage("push the docker Hub"){
       steps{
          echo "Push the image to docker hub"
           withCredentials([usernamePassword(credentialsId:"dockerHub",passwordVariable:"dockerHubPass",usernameVariable:"dockerHubUser")]){
           sh "docker tag server1 ${env.dockerHubUser}/server5:latest"
           sh "docker login -u ${env.dockerHubUser} -p ${env.dockerHubPass} "
           sh "docker push ${env.dockerHubUser}/server5:latest"
           }

           }
     }
     stage("deploy code"){
         steps{
          echo "I did it"
              }
        }
  }
}
